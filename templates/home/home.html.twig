{% extends 'base.html.twig' %}

{% block body %}

<div class="container-fluid bg-primary">
            <div class="container myNavbar py-0">
                <nav
                    class="navbar navbar-expand-lg bg-body-tertiary align-items-end py-0"
                >
                    <div class="container">
                        <a class="navbar-brand pb-0 goHome" href="{{ path('accueil') }}">
                            <img
                                src="{{ asset('index/') }}newlogo3.webp"
                                alt=""
                                height="60"
                                class="d-inline-block align-text-center"
                            />
                        </a>
                        <button
                            class="navbar-toggler"
                            type="button"
                            data-bs-toggle="collapse"
                            data-bs-target="#navbarSupportedContent"
                            aria-controls="navbarSupportedContent"
                            aria-expanded="false"
                            aria-label="Toggle navigation"
                        >
                            <span class="navbar-toggler-icon"></span>
                        </button>
                        <div
                            class="collapse navbar-collapse"
                            id="navbarSupportedContent"
                        >
                            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                                <li class="nav-item mx-2">
                                    <a
                                        class="nav-link active goHome"
                                        aria-current="page"
                                        href="{{ path('accueil') }}"
                                        >Accueil</a
                                    >
                                </li>
                                <li class="nav-item mx-1 dropdown">
                                    <a
                                        class="btn btn-success p-1 mt-1 text-primary nav-link dropdown-toggle"
                                        id="productTrigger"
                                        href="{{ path('homeCategories') }}"
                                        role="button"
                                        {# data-bs-toggle="dropdown" #}
                                        aria-expanded="false"
                                    >
                                        Produits
                                    </a>
                                    <div
                                        class="dropdown-menu px-2 py-2 mt-0 mb-1 myDropdown bg-light"
                                    >
                                        <div class="row text-center">
                                        {% for category in categories %}
                                            <div class="col-lg-3 p-1">
                                                <a
                                                    class="dropdown-item px-2"
                                            id="{{ category.alias }}"
                                                    href="{{ path( 'homeCategory', {id: category.id} ) }}">
                                                    <img
                                                        src="{{ asset('upload/') }}{{ category.pictureNormal }}"
                                                        width="100%"
                                                        class="navImg"
                                                    />
                                                    <p class="my-0 fw-bold">
                                                        {{ category.title }}
                                                    </p></a>
                                            </div>
                                        {% endfor %}
                                        </div>
                                    </div>
                                </li>
                                <li class="nav-item mx-1">
                                    <a
                                        class="nav-link goHome"
                                        href="#aloe-vera-forever"
                                        >Aloe Vera & Forever</a
                                    >
                                </li>
                                <li class="nav-item mx-1">
                                    <a
                                        class="nav-link goHome"
                                        href="#devenirFbo"
                                        >Devenir FBO</a
                                    >
                                </li>
                                <li class="nav-item mx-1">
                                    <a class="nav-link goHome" href="#commander"
                                        >Commander</a
                                    >
                                </li>
                                {# <li class="nav-item mx-1">
                                    <a
                                        class="nav-link"
                                        href="mailto:vluncasu@yahoo.com"
                                        >Contacter-moi</a
                                    >
                                </li> #}
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="{{ path('listProduct') }}" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    BackOffice
                                    </a>
                                    <ul class="dropdown-menu">
                                        <li><a class="dropdown-item" href="{{ path('addProduct') }}">Ajout produit</a></li>
                                        <li><a class="dropdown-item" href="{{ path('listProduct') }}">Liste des produits</a></li>
                                        <li><a class="dropdown-item" href="{{ path('addCategory') }}">Ajout categorie</a></li>
                                        <li><a class="dropdown-item" href="{{ path('listCategory') }}">Liste des categories</a></li>
                                        <li><a class="dropdown-item" href="{{ path('addCarousel') }}">Ajout images carousel</a></li>
                                        <li><a class="dropdown-item" href="{{ path('listCarousel') }}">Liste images carousel</a></li>
                                    </ul>
                                </li>

                                <li class="nav-item mx-1 mt-1">
                                    <a
                                        role="button"
                                        class="btn btn-danger text-primary fw-bolder"
                                        href=""
                                        >Boutique en ligne</a
                                    >
                                </li>
                            </ul>
                            <!-- <form class="d-flex" role="search">
                                <input
                                    class="form-control me-2"
                                    type="search"
                                    placeholder="Search"
                                    aria-label="Search"
                                />
                                <button class="btn btn-outline-success" type="submit">
                                    Search
                                </button>
                            </form> -->
                        </div>
                    </div>
                </nav>
            </div>
        </div>





{% endblock %}




{% block title  %}
{% endblock %}